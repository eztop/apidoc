syntax = "proto3";

package env;

option go_package = "github.com/eztop/apidoc/golang/env";

// Env Env
enum Env {
  // ENV_UNDEFINED 未定义
  ENV_UNDEFINED = 0;
  // ENV_DEV 开发环境
  ENV_DEV = 1;
  // ENV_QA 测试环境
  ENV_QA = 2;
  // ENV_PL 预生产环境
  ENV_PL = 3;
  // ENV_ONLINE 线上环境
  ENV_ONLINE = 4;
  // ENV_FAT 功能验收测试环境
  ENV_FAT = 5;
  // ENV_PERF_TEST 压测环境
  ENV_PERF_TEST = 6;
}